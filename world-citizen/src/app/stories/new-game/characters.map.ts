import { CharacterNames } from '../../characters/character.name';

export const CHARACTERS = {
  pilot: CharacterNames.pilot,
  angela: CharacterNames.angelaWhite,
  dante: CharacterNames.danteCole,
}
